<?xml version="1.0"?>
<package format="2">
  <name>gazebo_link_attacher</name>
  <version>0.1.0</version>
  <description>
    Welds two Gazebo links together with a runtime joint, on request. Needed because the
    2F-140's mimic joints are driven kinematically (SetPosition), so the fingers cannot exert
    grip force and gazebo_grasp_fix -- which attaches only on opposing contact forces -- can
    never fire on this gripper.
  </description>
  <maintainer email="dujun173@gmail.com">JunDu</maintainer>
  <license>BSD</license>

  <buildtool_depend>catkin</buildtool_depend>

  <depend>roscpp</depend>
  <depend>gazebo_ros</depend>

  <build_depend>message_generation</build_depend>
  <exec_depend>message_runtime</exec_depend>
</package>
